# Runtime dependencies for the deploy pipeline (python -m deploy).
# Pinned to the versions the pipeline was validated against.
#
# ipor-fusion is the public IPOR Fusion Python SDK: https://github.com/IPOR-Labs/ipor-fusion.py
# The pipeline needs `ipor_fusion.core.fusion_factory` and `ipor_fusion.core.context`
# (present since 3.x on PyPI).
ipor-fusion==3.6.3
web3==7.16.0
eth-abi==5.2.0
eth-utils==6.0.0
jsonschema==4.26.0
python-dotenv==1.2.2
